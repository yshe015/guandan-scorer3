services:
  guandan-scorer3:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - TZ=Pacific/Auckland
    restart: unless-stopped
